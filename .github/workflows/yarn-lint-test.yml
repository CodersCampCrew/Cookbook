name: yarn-lint-test
on:
  pull_request:
    branches:
       dev
       feature
       feature/adding-gh-actions

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       name: run yarn lint and test
       run: yarn lint
       run: yarn test
